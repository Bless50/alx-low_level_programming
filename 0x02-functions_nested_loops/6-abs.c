#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * Description:  Printing the absolute value of an integer
 *
 * Return - Always 0.
 */
int _abs(int x) 
{

if (x < 0)
{
return (-x);
}
else
{
return (x);
}
}
