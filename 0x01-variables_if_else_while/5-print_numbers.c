#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the letters of the alphabet in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;

while (i <=9 )
{
printf(" %d", i);
	i++;
}
printf("\n");

return (0);
}

